info:
  name: Probabilities
  type: http
  seq: 2

http:
  method: GET
  url: http://51.250.0.63:5004/api/Advertisements/probabilities
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
